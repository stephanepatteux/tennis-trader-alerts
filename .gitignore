# Secrets — never commit real keys
.env
.env.*
!.env.example

# Local per-user rules (example file is tracked)
data/rules.json
data/*.tmp
*.tmp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Editor / OS
.DS_Store
